/**
 * Custom Hooks
 *
 * Main barrel file for all custom React hooks.
 */

export { useMounted } from "./use-mounted";
export { useLocalStorage } from "./use-local-storage";
export {
  useMediaQuery,
  useIsMobile,
  useIsTablet,
  useIsDesktop,
  usePrefersDarkMode,
  usePrefersReducedMotion,
} from "./use-media-query";
