import type { DocsThemeConfig } from "nextra-theme-docs";

const config: DocsThemeConfig = {
  logo: <span style={{ fontWeight: 700 }}>NexusGen Docs</span>,
  project: {
    link: "https://github.com/nexusgen",
  },
  docsRepositoryBase: "https://github.com/nexusgen",
  footer: {
    content: "NexusGen Documentation",
  },
};

export default config;
